# set keymap
~/bin/set-keymap

# start dunst
#dunst > $logdir/dunst.log 2>&1  &

# start redshift
#redshift > $logdir/redshift.log 2>&1 &

# start unclutter
#unclutter > $logdir/unclutter.log 2>&1 &

exec stumpwm
